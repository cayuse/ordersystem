<h1>Editing <%= @company.name %></h1>
<br>

<% form_for(@company) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => { :f => f, :company => @company } %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<br>
<%= link_to 'Back', companies_path %>
